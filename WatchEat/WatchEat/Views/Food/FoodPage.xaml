<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" x:Name="PageRef"
             xmlns:ctrl="clr-namespace:WatchEat.Views.Controls"
             x:Class="WatchEat.Views.Food.FoodPage" Title="{Binding Title}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel"  Command="{Binding Cancel}" />
        <ToolbarItem  Text="Save"  Command="{Binding Save}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="20" Padding="15">
                <Label Text="Name" FontSize="Medium" />
                <Entry Text="{Binding Path=Product.Name}" FontSize="Small" />
                <Label Text="Calories" FontSize="Medium" />
                <ctrl:NumericEntry NumericValue="{Binding Path=Product.Calories}" FontSize="Small"/>
                <Label Text="Carbs" FontSize="Medium" />
                <ctrl:NumericEntry NumericValue="{Binding Path=Product.Carbs}" FontSize="Small"/>
                <Label Text="Protein" FontSize="Medium" />
                <ctrl:NumericEntry NumericValue="{Binding Path=Product.Protein}" FontSize="Small"/>
                <Label Text="Fat" FontSize="Medium" />
                <ctrl:NumericEntry NumericValue="{Binding Path=Product.Fat}" FontSize="Small"/>
                <Label Text="Fiber" FontSize="Medium" />
                <ctrl:NumericEntry NumericValue="{Binding Path=Product.Fiber}" FontSize="Small"/>
                <Label Text="Salt" FontSize="Medium" />
                <ctrl:NumericEntry NumericValue="{Binding Path=Product.Salt}" FontSize="Small"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>