<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" x:Name="PageRef"
             xmlns:ctrl="clr-namespace:WatchEat.Views.Controls"
             x:Class="WatchEat.Views.EventSelection.WaterPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel"  Command="{Binding Cancel}" />
        <ToolbarItem  Text="Add"  Command="{Binding Add}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Time" FontSize="Medium"/>
            <TimePicker Time="{Binding SelectedTime}" />
            <Label Text="Water Amount" FontSize="Medium" />
            <ctrl:NumericEntry NumericValue="{Binding Amount}" FontSize="Small"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>