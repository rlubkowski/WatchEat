<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="WatchEat.Views.EventSelection.EventSelectionPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" Text="Back"  Command="{Binding Cancel}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Water" Command="{Binding AddWaterEvent}"/>
            <Button Text="Food"  Command="{Binding AddFoodEvent}"/>
            <Button Text="Training"  Command="{Binding AddActivityEvent}"/>
            <Button Text="Weight" Command="{Binding AddWeightEvent}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>